# Relayer EOA private key (funds deployments & executes txs)
# Generate with: cast wallet new
# Fund with Base Sepolia ETH from https://www.alchemy.com/faucets/base-sepolia
# ⚠️ VITE_ prefix exposes this in the browser bundle — PoC only!
VITE_RELAYER_PRIVATE_KEY=0x...
